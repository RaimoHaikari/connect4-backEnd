class Settings {

    static FREE = 0;

    static PLAYER_TURN = 0;
    static PLAYER_PIECE = 1;
    static PLAYER_MAX_SCORE = 1000000;  

    static AI_TURN = 1;
    static AI_PIECE = -1;
    static AI_MAX_SCORE = -1000000;     

    static DRAW_SCORE = 0;

    static DE_SMET_MAX_SCORE = 1000000;

    static WINNING_LENGTH = 4;

    static ROWS = 6;
    static COLS = 7;

    static SEARCH_DEPTH = 2;

    static PIECE_IN_CENTER_COL = 3;

}

module.exports = Settings